const fs = require("fs");
const input = fs.readFileSync("ex.txt").toString().split("\n");

const TESTCASE = input[0];
let temp = "";
console.log(input)
for(let i=1; i<=TESTCASE; i++) {
    temp = input[i]
    

}